<controls:MetroWindow x:Class="FindRomCover.SettingsWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      xmlns:componentModel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
                      mc:Ignorable="d"
                      Title="Edit Supported Extensions" Height="450" Width="400"
                      WindowStartupLocation="CenterScreen">
    <controls:MetroWindow.Resources>
        <CollectionViewSource x:Key="SortedExtensions" Source="{Binding SupportedExtensions}" >
            <CollectionViewSource.SortDescriptions>
                <componentModel:SortDescription PropertyName="." Direction="Ascending"/>
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </controls:MetroWindow.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,10"
                   AutomationProperties.Name="Instructions"
                   AutomationProperties.HelpText="Manage the list of supported ROM file extensions"
                   ToolTip="Add or remove file extensions that will be recognized as ROM files">
            Manage the list of supported ROM file extensions.
        </TextBlock>

        <ListBox Grid.Row="1"
                 x:Name="LstSupportedExtensions"
                 BorderBrush="Gray"
                 BorderThickness="1"
                 ItemsSource="{Binding Source={StaticResource SortedExtensions}}"
                 AutomationProperties.Name="Supported Extensions List"
                 AutomationProperties.HelpText="List of file extensions that will be recognized as ROM files"
                 ToolTip="List of file extensions recognized as ROM files"
                 TabIndex="1" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
            <Button x:Name="BtnAdd" Content="Add..." Width="100" Margin="0,0,10,0" Click="BtnAdd_ClickAsync"
                    AutomationProperties.Name="Add Extension"
                    AutomationProperties.HelpText="Add a new file extension to the list"
                    ToolTip="Add a new file extension to the supported list"
                    TabIndex="2" />
            <Button x:Name="BtnRemove" Content="Remove" Width="100" Click="BtnRemove_Click"
                    IsEnabled="{Binding ElementName=LstSupportedExtensions, Path=SelectedItem, Converter={StaticResource IsNotNullConverter}}"
                    AutomationProperties.Name="Remove Extension"
                    AutomationProperties.HelpText="Remove the selected file extension from the list"
                    ToolTip="Remove the selected file extension from the list"
                    TabIndex="3" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="BtnSave" Content="Save" Width="100" Margin="0,0,10,0" Click="BtnSave_Click" IsDefault="True"
                    AutomationProperties.Name="Save Extensions"
                    AutomationProperties.HelpText="Save the list of extensions"
                    ToolTip="Save the list of supported extensions and close"
                    TabIndex="4" />
            <Button x:Name="BtnCancel" Content="Cancel" Width="100" IsCancel="True" Click="BtnCancel_Click"
                    AutomationProperties.Name="Cancel"
                    AutomationProperties.HelpText="Discard changes and close"
                    ToolTip="Discard changes and close this window"
                    TabIndex="5" />
        </StackPanel>
    </Grid>
</controls:MetroWindow>
